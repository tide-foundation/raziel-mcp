# Gap Register

Uncertainties, missing information, and open questions from source audit.
Updated: 2026-04-13 (full sync pass: status count corrected, no new gaps)

Controlled status set:
- `RESOLVED_BY_KEYLESSH` — fully answered by exemplar evidence; no runtime confirmation needed
- `RESOLVED_BY_VENDOR` — answered by vendor clarification; suitable for operational drafting
- `RESOLVED_BY_NORMATIVE_DOC` — answered by normative documentation (e.g., CHANGE_REQUEST_API.md)
- `RESOLVED_BY_MULTI_EXEMPLAR` — answered by consistent evidence across two or more exemplars
- `PARTIALLY_RESOLVED_BY_KEYLESSH` — exemplar provides partial evidence; gaps remain
- `PARTIALLY_RESOLVED_BY_MULTI_EXEMPLAR` — multiple exemplars provide partial evidence; gaps remain
- `PARTIALLY_RESOLVED_BY_VENDOR` — vendor confirmed the direction/decision; some detail (e.g., exact payloads) still needed before full drafting
- `RESOLVED_BY_RUNTIME_TEST` — answered by probing a live instance and recording the result; the strongest class
- `STILL_UNRESOLVED` — no evidence from any source
- `DOCS_ONLY_CONTRADICTION` — docs say one thing, implementation says another
- `REQUIRES_RUNTIME_VALIDATION` — exemplar suggests an answer but single-app evidence is insufficient to generalize

---

## Gap Table

| Gap ID | Description | Status | Evidence Source | Next Action |
|--------|------------|--------|-----------------|-------------|
| GAP-001 | Authentication API omits all Tide-specific flows | RESOLVED_BY_VENDOR | `init-tidecloak.sh` reveals undocumented admin endpoints. `server/auth.ts` shows DPoP verification. `canon/tidecloak-endpoints.md` now documents 94 endpoints across 8 resource classes (including the token-exchange endpoint). | Resolved. All Tide-specific auth endpoints documented in `canon/tidecloak-endpoints.md`. Source API docs remain incomplete but pack has full operational coverage. |
| GAP-002 | Management API omits IGA endpoints | RESOLVED_BY_NORMATIVE_DOC | CHANGE_REQUEST_API.md provides complete REST API with request/response schemas for all change-set operations, policy management, TS client. | Resolved. Canon updated. |
| GAP-003 | Quorum threshold formula inconsistency | PARTIALLY_RESOLVED_BY_KEYLESSH | `start.sh`: threshold is deployment-configurable. Three distinct concepts: admin quorum (70%), Fabric operational threshold (T/N), SSH policy threshold (per-role). | Document the three-layer distinction. CHANGE_REQUEST_API.md confirms SSH policy `threshold` is distinct from IGA admin quorum. |
| GAP-004 | Error handling undocumented across all SDKs | RESOLVED_BY_VENDOR | No formal error code taxonomy. One custom class: `NetworkError` (extends `Error`, includes `.response`). All other errors are plain `Error` with string messages. Retryable: `NetworkError`, `enclave.networkFailure`, `enclave.throttled`, `enclave.thresholdTimeoutFailure`, `authRefreshError` (once). Fatal: init errors, input validation, missing roles (`"User has not been given any access to '${tag}'"`), policy violations (`BadPolicy.*`, `OutOfGasException`, `PolicyDecision.Deny()`), mode restrictions. Token/auth errors require re-login. Event listeners (`authError`, `authRefreshError`, `initError`) are the primary async error channel. No built-in retry logic in SDK. | Resolved. Canon updated with error catalog. Adapters pending. |
| GAP-005 | `npm init @tidecloak/nextjs@latest` behavior undocumented | RESOLVED_BY_VENDOR | Vendor clarification provided complete workflow. forseti-crypto-quickstart confirms `create-forseti-nextjs` scaffolding pattern. | Resolved. |
| GAP-006 | Token lifetime and session timeout defaults | RESOLVED_BY_VENDOR | `accessTokenLifespan: 600` confirmed in all four exemplar realm.json files. | Resolved. |
| GAP-007 | Forseti gas metering undefined | RESOLVED_BY_VENDOR | Gas metering documented as part of GAP-008 five-layer sandbox model: default 50,000 gas limit. `Claim(key)` = 5 gas, `Log(message)` = 25-30 gas, `ChargeTime()` = units per ms. Throws `OutOfGasException` when exhausted. OS-level CPU/memory limits also enforced via VmHost rlimits. | Resolved. Already incorporated into canon Forseti section. |
| GAP-008 | Forseti sandbox namespace blocklist incomplete | RESOLVED_BY_VENDOR | Five-layer sandbox: Roslyn compilation, IL vetting (block-list: `System.IO`, `System.Net`, `System.Diagnostics`, `System.Threading`, `System.Reflection`, `System.Runtime.InteropServices`, `System.Reflection.Emit`, `Microsoft.Win32`; hard-blocked: `System.Console`, `System.Runtime.CompilerServices.Unsafe`; non-determinism blocked by default), AssemblyLoadContext isolation, VmHost process isolation (OS rlimits), gas metering (default 50k). Compile succeeds but IL vetting rejects with `BadPolicy.ForbiddenCall`. Optional allow-list mode; forbidden always wins. | Resolved. Canon updated. |
| GAP-009 | Session key lifecycle undocumented | RESOLVED_BY_VENDOR | Two distinct concepts: (1) Data encryption key — fresh per `doEncrypt()` call, no reuse. <32B: fresh ElGamal random scalar. >=32B: fresh 32-byte AES key via `crypto.getRandomValues()`, used once, discarded. No key state persists after call returns. (2) Session key (`t.ssk`) — Ed25519 key pair in doken, private component lives in ORK enclave iframe. Created at login, destroyed on tab close/logout. Authenticates user to ORKs but does NOT encrypt data. Enclave is singleton on `TideCloak` instance, created lazily or eagerly at init. Multi-tab: each tab has independent enclave. Session key mismatch triggers full re-login. Tidewarden `tide_encrypted_key` is app-specific, not SDK standard. | Resolved. Canon updated. Adapters have no encryption key state to manage. |
| GAP-010 | AzureAD integration incomplete | STILL_UNRESOLVED | No exemplar uses AzureAD. | Still needs TideCloak-side federation setup. |
| GAP-011 | Grafana integration incomplete | STILL_UNRESOLVED | No exemplar uses Grafana. | Still needs complete procedure. |
| GAP-012 | Silent SSO failure behavior | PARTIALLY_RESOLVED_BY_KEYLESSH | `AuthContext.tsx`: corrupted state detection, `?reset=true`, 10s timeout. | Document defensive startup pattern. Iframe-specific failure modes still undocumented. |
| GAP-013 | E2EE payload size limits | RESOLVED_BY_VENDOR | No application-level max. Payloads <32 bytes use ElGamal directly; >=32 bytes use hybrid AES-256-GCM + ElGamal. Both `Uint8Array` (binary) and strings supported. Practical ceiling is ASP.NET Core Kestrel default (~28.6 MB request body). Self-encryption and policy-based encryption share the same size characteristics. | Resolved. Canon updated. Recommend boundary eval test (e.g., 10 MB payload). |
| GAP-014 | Disaster recovery procedures | STILL_UNRESOLVED | No exemplar has DR procedures. | Still needs recovery runbook. |
| GAP-015 | "BEd25531" curve name typo | RESOLVED_BY_WHITEPAPER | Whitepaper consistently uses "BEd255475" across 6+ files. Architecture.md had a typo ("BEd25531"). Corrected. | Resolved. Architecture.md fixed. |
| GAP-016 | PRISM throttling thresholds | STILL_UNRESOLVED | No exemplar configures PRISM throttling. | Still needs throttle configuration docs. |
| GAP-017 | Doken lifecycle | RESOLVED_BY_VENDOR | Doken `exp` copied directly from SSO access token `exp` at issuance — they share the same expiration. Auto-reissued on token refresh; SDK handles transparently including mid-operation refresh via `dokenRefreshCallback`. No independent TTL config. Revocation operates at signature level: EdDSA signature added to per-user blocklist in `RevokedAuthorizerService`. Role claims (`realm_access`, `resource_access`) are snapshot at issuance, only update on next refresh — explains 120s delay in F6. Fully-expired doken cannot be refreshed (must reauthenticate). Structure: `{ alg: "EdDSA", typ: "doken" }`, payload includes `t.ssk`, `tideuserkey`, `vuid`, `t.uho`, `exp`, `aud` (VVKID), `realm_access`, `resource_access`. | Resolved. Canon updated. |
| GAP-018 | Contract debugging and error propagation | RESOLVED_FOR_EVALS | keylessh: errors thrown on failed sign. CHANGE_REQUEST_API.md: Forseti contract CRUD documented; `contractHash` as SHA512. forseti-crypto-quickstart: `PolicyDecision.Deny("message")` as error surface. test-cases F11: denial messages propagated to client. Batch-02 Q-01 documented ORK error codes that propagate to client: `BadPolicy.ForbiddenCall:{target}`, `BadPolicy.EntryTypeNotFound`, `BadPolicy.BudgetExceeded`, `VmHost.Timeout`, `OutOfGasException`. These arrive as opaque strings, not structured errors. | Client-visible error catalog sufficient for eval pass/fail criteria. Roslyn/IL diagnostics never surface to client — compilation-error evals are stretch goals asserting failure-to-sign only. See `notes/eval-blocker-resolution.md`. |
| GAP-019 | Key healing limits | STILL_UNRESOLVED | No exemplar configures key healing. | Still needs staleness threshold docs. |
| GAP-020 | Account recovery email details | STILL_UNRESOLVED | No exemplar implements account recovery. | Still needs email delivery docs. |
| GAP-021 | Ragnarok post-transition details | STILL_UNRESOLVED | No exemplar implements Ragnarok. | Still needs transition plan. |
| GAP-022 | C# SDK lacks Tide-specific content | PARTIALLY_RESOLVED_BY_MULTI_EXEMPLAR | Three contracts across two exemplars demonstrate comprehensive C# API surface: `IAccessPolicy`, `DataContext`, `ExecutorContext`, `ApproversContext`, `PolicyDecision`, `DokenDto`, `DokenDto.WrapAll()`, `Decision.*`, `TideKey.From()`, `Utils.GetEpochSeconds()`, `ExecutionType`, `ApprovalType`. | Not a standalone SDK package — types are available only within Forseti contract context. |
| GAP-023 | Keycloak-to-TideCloak migration path | STILL_UNRESOLVED | No exemplar is a migration. All are greenfield. | Still needs migration guide. |
| GAP-024 | Multi-tenant / multi-realm Fabric interaction | RESOLVED_BY_MULTI_EXEMPLAR | Ratidefy SaaS (ratidefy.com) implements full multi-tenant with per-tenant realms. Two-phase provisioning: realm+user first, role assignment after Tide account link. Path-based routing (`/t/<slug>/`). Each tenant gets independent VVK, Forseti policies, and IGA. Dynamic adapter config served from DB. Key learnings: client-origin-auth key format, IDP signing requirement, role assignment ordering. | Resolved. `canon/tidecloak-bootstrap.md` updated with "Hosted / SaaS Multi-Tenant Provisioning" section. |
| GAP-025 | VVK ORK 12-gate verification details | STILL_UNRESOLVED | No exemplar reveals gate list. | Still needs full gate list and error behavior. |
| GAP-026 | Adapter JSON field documentation | RESOLVED_BY_KEYLESSH | `tidecloakConfig.ts` TypeScript interface. forseti-crypto-quickstart provides additional field: `client-origin-auth-*` (signed origin binding). | Record as canon. Additional field `client-origin-auth-*` noted (GAP-048). |
| GAP-027 | Production deployment guide | PARTIALLY_RESOLVED_BY_KEYLESSH | keylessh `DEPLOYMENT.md` shows container patterns. tidewarden shows Docker multi-stage builds + Dockerfile.acr for ACR. | Extract reusable patterns. Full checklist still needed. |
| GAP-028 | CSP requirements for Tide SWE undocumented | RESOLVED_BY_VENDOR | Vendor clarification: `frame-src '*'` required for ORK re-homing. | Resolved. |
| GAP-029 | Tide-specific admin endpoints not in API docs | RESOLVED_BY_VENDOR | keylessh: `setUpTideRealm`, `toggle-iga`, `get-required-action-link`, `get-installations-provider`. forseti-crypto-quickstart: `sign-idp-settings`, `identity-provider/instances/tide` GET/PUT. tidewarden: `init-cert` accepts `initCertSig` + `doken`; voucher path `tidevouchers/fromUserSession?sessionId={sid}`. CHANGE_REQUEST_API.md: change-set schemas now documented. `setUpTideRealm` fully documented (vendor confirmation, batch-02 Q-03). `get-required-action-link` documented in `canon/tidecloak-endpoints.md`: `POST .../tideAdminResources/get-required-action-link?userId={id}&lifespan=43200`, body `["link-tide-account-action"]`. | Resolved. All admin endpoints documented in `canon/tidecloak-endpoints.md`. |
| GAP-030 | Account linking onboarding step undocumented | RESOLVED_BY_VENDOR | Vendor clarification confirmed scope. test-cases F2 provides full E2E account linking test. | Resolved. |
| GAP-031 | `_tide_enabled` role requirement undocumented | RESOLVED_BY_VENDOR | Vendor clarification: `setUpTideRealm` runs after realm import and does not auto-create `_tide_enabled`. Declare `_tide_enabled` in the `realm.json` template so the role already exists when setup continues. If omitted, it must be created and assigned manually afterward. Exemplars that omit it may rely on post-import setup, a different provisioning path, or incomplete checked-in realm templates. | Resolved. Update canon and templates to declare `_tide_enabled` in realm.json. |
| GAP-032 | DPoP server-side verification pattern undocumented | RESOLVED_BY_VENDOR | Both ES256 and EdDSA supported. ES256 is the default. Algorithm negotiated at runtime via server's `dpop_signing_alg_values_supported` OIDC metadata. Apps may override with `useDPoP.alg`. EdDSA has automatic fallback logic if Ed25519 key generation fails. ES384/ES512 declared in types but not implemented (would throw). | Resolved. Update canon DPoP section with canonical guidance: ES256 default, EdDSA supported. |
| GAP-033 | Adapter JSON JWKS embedding undocumented | RESOLVED_BY_VENDOR | `jwk` field present when IGA is enabled on the realm (`isIGAEnabled == true` and `tide-vendor-key` component exists). Confirmed across all exemplar adapter JSON files. Corrected by batch-02 Q-04: jwk injection is conditional on IGA. | Resolved. Canon updated. |
| GAP-034 | Threshold is deployment-configurable (not fixed 14/20) | RESOLVED_FOR_TEMPLATES | All exemplars use T=3/N=5 for test. Docs/whitepaper present 14/20 as if fixed. Canon (I-02) and deploy playbook already document `THRESHOLD_T`/`THRESHOLD_N` env vars. Template guidance settled in `notes/template-blocker-resolution.md`. | Resolved for template purposes. Docs-only contradiction remains in whitepaper but does not block code generation. |
| GAP-035 | Change-set API workflow undocumented | RESOLVED_BY_NORMATIVE_DOC | CHANGE_REQUEST_API.md provides full lifecycle (DRAFT->PENDING->APPROVED->ACTIVE), all endpoints, all types, TypeScript client. | Resolved. Canon updated. |
| GAP-036 | Tide protocol mappers not documented for realm setup | RESOLVED_BY_VENDOR | Vendor confirmed auto-creation. All exemplar realm.json files include mappers. | Resolved. |
| GAP-037 | Corrupted auth state recovery pattern undocumented | PARTIALLY_RESOLVED_BY_KEYLESSH | keylessh `AuthContext.tsx` localStorage scan, `?reset=true`. | Document as recommended SPA pattern, not Tide requirement. |
| GAP-038 | `add-rejection` uses multipart/form-data while sign/commit/cancel use JSON | RESOLVED_BY_NORMATIVE_DOC | CHANGE_REQUEST_API.md documents the asymmetry. tidecloakApi.ts (forseti-crypto-quickstart) confirms `addApproval` uses FormData. Both `add-review` and `add-rejection` use `multipart/form-data`, all other mutation endpoints use JSON. | Resolved. Canon and playbooks already document the content-type constraint. Adapters pending. LOW severity. |
| GAP-039 | `initCert` format and generation procedure undocumented | RESOLVED_BY_VENDOR | Endpoint: `POST /admin/realms/{realm}/tide-iga-provider/role-policy/{roleId}/init-cert`. JSON body: `initCert` (required, base64-encoded signed policy bytes) and `initCertSig` (optional, base64-encoded VVK signature). No `doken` field — endpoint is pure storage, persists to `TideRoleDraftEntity`. Returns 200 `{"message":"initCert updated for role <roleId>"}`, 404 if role not found, 400 if initCert missing. Consumed downstream during multi-admin change-set signing to produce vendor-signed access proofs. Note: path is `tide-iga-provider`, not `tide-admin`. JS admin client does not have this wired up — must use raw HTTP. | Resolved. Canon updated. Adapters pending. |
| GAP-040 | SSH policy `threshold` vs IGA admin quorum are distinct concepts | PARTIALLY_RESOLVED_BY_MULTI_EXEMPLAR | CHANGE_REQUEST_API.md: SSH policy `threshold` is per-role approval count. SetupIGA.md: admin quorum is `max(1, floor(N*0.7))`. test-cases F4 confirms admin policy threshold=1 for creating new policies, while new policy has threshold=2 for later use. | Canon must clarify these are independent. |
| GAP-041 | Which admin actions trigger draft creation vs immediate execution | RESOLVED_BY_VENDOR | 18 specific admin API actions create drafts when IGA is enabled on a non-master realm. Not all mutating endpoints — a finite set via custom JPA Model Provider SPIs. Two categories: DRAFT (requires quorum sign/commit) and ACTIVE (auto-approved, audit-only). DRAFT actions: create user, grant/revoke role to user, add/remove user from group, delete role (if has users), add/remove composite role, grant/revoke role to group, move group, create client (when users exist), toggle client full-scope, update client scope mappings, trigger license rotation, policy changes from user-role mappings. ACTIVE actions: create realm role, create client role. Master realm and IGA-disabled realms are exempt. | Resolved. Canon updated with full list. |
| GAP-042 | `@tideorg/js` vs `@tidecloak/js` for Models/Contracts imports | RESOLVED_BY_MULTI_EXEMPLAR | forseti-crypto-quickstart and tidewarden both import `Models` and `Contracts` from `@tideorg/js`, not `@tidecloak/js`. Webpack workaround in forseti-crypto-quickstart explicitly notes re-export mismatch. Canon is wrong. | Fix canon SDK Internals section. |
| GAP-043 | Two encryption role naming patterns: `self*` vs non-self | RESOLVED_BY_MULTI_EXEMPLAR | test-cases F7: `_tide_<tag>.selfencrypt`/`selfdecrypt` for self-encryption (user-bound, no policy bytes). test-cases F9: `_tide_<tag>.encrypt`/`decrypt` for policy-based encryption (shared, requires policy bytes). Canon only documents the self-variant. | Add non-self pattern to canon Voucher Gates and E2EE sections. |
| GAP-044 | `providerId` discrepancy for adapter export | RESOLVED_BY_VENDOR | Only one valid provider ID: `keycloak-oidc-keycloak-json`. The string `tidecloak-oidc-keycloak-json` does not exist in the codebase. TideCloak post-processes the standard provider output via the vendor endpoint (`get-installations-provider`) to inject Tide fields (`vendorId`, `jwk`, `homeOrkUrl`, `client-origin-auth-*`). The `jwk` field is only added when IGA is enabled. Any reference to `tidecloak-oidc-keycloak-json` is a typo or outdated. | Resolved. Fix keylessh reference in canon and templates. Use `keycloak-oidc-keycloak-json` everywhere. |
| GAP-045 | `_tide_enabled` absent from all new exemplar realm.json files | RESOLVED_BY_VENDOR | Vendor clarification: declare `_tide_enabled` in realm.json template. Exemplars that omit it may rely on post-import provisioning or have incomplete checked-in templates. See GAP-031. | Resolved. Aligned with GAP-031. |
| GAP-046 | `GenericResourceAccessThresholdRoleContract` undocumented | RESOLVED_BY_VENDOR | Built-in default contract (ID: `GenericResourceAccessThresholdRole:1`). One of four pre-registered contracts in the ORK factory. Policy params: `role` (string, client role name), `resource` (string, client/resource ID), `threshold` (number, min approvers). Only implements `ValidateApprovers` — iterates approver dokens checking `resource_access[resource].roles.includes(role)`, throws if count < threshold. `ValidateData` is no-op. `ValidateExecutor` not implemented (inherited stub throws). Checks **client roles** (not realm roles) — for realm roles use sibling `GenericRealmAccessThresholdRoleContract`. Use for simple role-gated multi-approval. Write custom contract for executor validation, data validation, or complex logic. | Resolved. Canon updated with default contract table. |
| GAP-047 | `CustomAdminUIDomain` + `sign-idp-settings` endpoint undocumented | RESOLVED_BY_VENDOR | `sign-idp-settings` is ALWAYS required after any Tide IDP config change (logo, background, backup toggle, custom admin domain, client origin changes). Signs all IDP settings with vendor key via ORK network. Without it, enclave rejects settings as unsigned. `CustomAdminUIDomain` is APP-SPECIFIC: only needed when a separate app (not built-in Admin Console) hosts the change-set approval UI or needs post-LinkTideAccount redirects. Signs 6 items: loginURL, linkTideURL, changeSetURL, CustomAdminUIDomain (if set), all client web origins, vendor settings. Requires active VRK/license. | Resolved. Update init playbook: `sign-idp-settings` is required step; `CustomAdminUIDomain` is conditional. |
| GAP-048 | `client-origin-auth-*` adapter JSON field undocumented | RESOLVED_BY_VENDOR | ORK-network-produced signature attesting a web origin is authorized for a client. Generated during `sign-idp-settings`: each client's allowed origins are included in the signing draft sent to ORKs via `Midgard.SignModel`; a per-origin signature is returned. Key format: `client-origin-auth-{full origin URL}`, value: base64-encoded signature. SDK requires it as `clientOriginAuth` for `RequestEnclave`/`ApprovalEnclave` initialization — without it, enclave init fails. Config loader must select entry matching `window.location.origin`. One entry per allowed origin per client; no browser-specific variants in TideCloak codebase. Internal storage key: `clientAuth:{clientId}{origin}`, stripped to `client-origin-auth-{origin}` in adapter JSON. | Resolved. Canon updated. |
| GAP-049 | DPoP algorithm: EdDSA vs ES256 | RESOLVED_BY_VENDOR | Both supported. ES256 is default. See GAP-032 for full details. Algorithm negotiated via `dpop_signing_alg_values_supported`; overridable with `useDPoP.alg`. EdDSA has fallback logic. ES384/ES512 not implemented. | Resolved. Aligned with GAP-032. |
| GAP-050 | DPoP verification uses `createRemoteJWKSet` in test-cases | RESOLVED_BY_VENDOR | Three distinct verification targets: (1) Access token JWT — use `createLocalJWKSet(config.jwk)` only. (2) DPoP proof — verified against ephemeral `jwk` embedded in the proof header per RFC 9449. (3) DPoP binding — `cnf.jkt` thumbprint comparison. **Pack policy**: `createRemoteJWKSet` is forbidden for this pack. If `jwk` is missing from adapter JSON, this is a setup/bootstrap failure — re-export adapter with IGA enabled. Do not fall back to remote JWKS. The test-cases exemplar uses `createRemoteJWKSet` but this pack treats it as an anti-pattern. | Resolved. I-04 updated to local-only (no remote fallback). |
| GAP-051 | Voucher endpoint `tidevouchers/fromUserSession` undocumented | RESOLVED_BY_VENDOR | TideCloak-specific endpoint from `tidecloak-key-provider` extension. For `@tidecloak/js` apps, SDK constructs voucher URL internally (`#getVoucherUrl()` reads `sid` from access token, builds `{realmUrl}/tidevouchers/fromUserSession?sessionId={sid}`). App developers do NOT need to construct this URL. Non-TideCloak integrations must provide `voucherURL` to Heimdall SDK directly. Endpoint does role-based voucher authorization: `signin`/`updateaccount` always allowed; `vendorsign` requires `_tide_*` or authorizer or `tide-realm-admin` role; `vendordecrypt` requires `_tide_*.selfdecrypt` role. Two sub-endpoints: `fromUserSession` (post-login operations) and `fromAuthSession` (login flows). | Resolved. Canon updated. Voucher concept is universal but this endpoint is TideCloak-specific. |
| GAP-052 | `tide_encrypted_key` rotation lifecycle undocumented | PARTIALLY_RESOLVED_BY_MULTI_EXEMPLAR | tidewarden stores per-user `tide_encrypted_key` (vault key encrypted by Tide). Client POSTs it on first setup, server returns it on every login/sync. | Key rotation, re-encryption after password change, and multi-device scenarios undocumented. |
| GAP-053 | Browser-specific `signedClientOrigin` variants | RESOLVED_BY_VENDOR | The `signedClientOriginChrome`/`signedClientOriginFirefox` pattern does NOT appear in the TideCloak codebase. TideCloak adapter JSON uses a single unified `client-origin-auth-{origin}` per origin with no browser-specific variants. The Tidewarden browser-specific fields are a Tidewarden-specific concern, not a TideCloak/ORK behavior. | Resolved. Tidewarden-specific; do not generalize. |
| GAP-054 | `TIDE_VOUCHER_BASE_URL` override for PNA tunneling | RESOLVED_BY_VENDOR | Tidewarden-specific deployment concern for Chrome Private Network Access (PNA) workaround. Not a universal Tide concept. Standard `@tidecloak/js` apps do not need this — SDK constructs voucher URLs automatically. | Resolved. Tidewarden-specific; do not include in canon or general skills. |
| GAP-055 | `oauth2-dpop` npm package for DPoP verification | PARTIALLY_RESOLVED_BY_MULTI_EXEMPLAR | test-cases `dpop-protected/route.ts` uses `oauth2-dpop` package for server-side DPoP proof verification. Simpler than manual verification. | Document as alternative to manual DPoP verification in canon. |
| GAP-056 | `deleteStatus` semantics for ACTIVE change-set records | STILL_UNRESOLVED | CHANGE_REQUEST_API.md: ACTIVE records have a `deleteStatus` field tracking pending deletion. Does deletion require the same quorum? | Needs vendor clarification. |
| GAP-057 | Licensing change requests use different path prefix | STILL_UNRESOLVED | CHANGE_REQUEST_API.md: `GET /tideAdminResources/change-set/licensing/requests` vs `GET /tide-admin/change-set/users/requests`. Path inconsistency suggests separate implementations. | Needs clarification on whether licensing requests follow the same sign/commit flow. |
| GAP-058 | Server-side delegation flow undocumented in pack | UNSHIPPED_REMOVED | Server-side delegation is an asgard feature (the `asgard-tide` Node SDK, as used by keylessh) + two-hop chain of trust (TideMtlsAuthenticator Java, DelegationTokenSignRequest C#); exists only on unmerged asgard/keylessh feature branches and ork `asgard-implementation`; nothing on `origin/main`. | Removed from pack 2026-07-07 — was documenting unshipped behavior. Re-document only after asgard-tide delegation merges to main. |
| GAP-059 | TideCloak extension endpoints not comprehensively documented | RESOLVED_BY_VENDOR | 94 endpoints across 8 resource classes (VendorResource, VoucherResource, PublicResource, TideIdpResource, TideIdpAdminRealmResource, IGARealmResource, TideAdminRealmResource, PolicyResourceProvider). | Resolved. Canon `tidecloak-endpoints.md` created with full endpoint reference. |
| GAP-060 | Server-side delegation SDK not documented in pack | UNSHIPPED_REMOVED | The delegation SDK is an asgard (`asgard-tide`) Node package, unmerged/experimental and unpublished; lives only on asgard/keylessh feature branches, not `main`. (There is no `@tidecloak/*` server package — that was a mis-attribution.) | Removed from pack 2026-07-07 — unshipped. |
| GAP-061 | Scoped delegation (requested_roles) undocumented | UNSHIPPED_REMOVED | Scoped-delegation role filtering is part of the unshipped asgard (`asgard-tide`) delegation surface (feature-branch only). | Removed from pack 2026-07-07 — unshipped. |
| GAP-062 | Custom Forseti contract creation guide missing from pack | RESOLVED_BY_VENDOR | Ork.Forseti.Sdk README, IAccessPolicy interface, DecisionBuilder, DokenDto, PolicyParam, ForsetiSdk runtime, sandbox restrictions, deployment, policy signing. | Resolved. Canon `custom-contracts.md` created. |
| GAP-063 | No Go/REST SDK for ORK threshold signing operations | STILL_UNRESOLVED | LEARNINGS-cosign-batch-001 L-01: ORK signing (`createTideRequest` → `executeSignRequest`) only available via JS SDK. No Go, Python, or REST API for direct ORK interaction. Resolved for web apps (JS SDK); blocks CLI tools and non-JS server-side signing. | Needs vendor clarification on ORK REST API or multi-language SDK plans. |
| GAP-064 | No CLI/terminal authentication flow (PRISM requires browser SWE iframe) | STILL_UNRESOLVED | LEARNINGS-cosign-batch-001 L-02: all TideCloak auth flows require browser context for PRISM zero-knowledge proof via SWE iframe. No device auth grant, service account, or headless flow exists. Browser-bridge (localhost page) is a workaround, not a native flow. | Needs vendor clarification on device auth grant or service account model. |
| GAP-065 | IGA/QEA change-request API surface migrated: `/iga/change-requests/...` replaces `/tide-admin/change-set/...` | RESOLVED_BY_VENDOR | **CONFIRMED by Tide (2026-07-07)** and spec obtained (`tide-foundation/tidecloak-iga-extensions/docs/qea-iga-api.md`). iga-core's `/iga/change-requests/...` surface replaces the legacy `/tide-admin/change-set/...`. Surface: write captured as HTTP 202 + change-request `id`; per-id `POST .../{id}/authorize` (body `{}`; 409=four-eyes) → `POST .../{id}/commit` (412=under threshold/unmet dependency); `POST .../{id}/deny` (204); batch `POST /iga/change-requests/bulk-authorize` (`{actionTypeIn,limit}`, 429 mutex); `GET /iga/change-requests?status=PENDING` (objects keyed by `id`, with `entityType`/`readyToCommit`/`threshold`/`dependsOn`); Tide-mode enclave two-phase `GET`/`POST .../{id}/approval-model`; comments + `POST /iga/adopt`. Toggle unchanged (`POST /tide-admin/toggle-iga`). Two-mode split — Tide (`iga.attestor=tide`, cryptographic VRK→Midgard→ORK) vs Tideless (`iga.attestor=simple`/unset, username attestation, NO crypto). | **Resolved.** New authoritative reference `canon/iga-change-requests-api.md` created (full spec + old→new map + bootstrap authorize→commit loop). Reconciled across: `canon/feature-mapping.md`, `canon/invariants.md` (I-10), `canon/tidecloak-endpoints.md`, `canon/concepts.md`, `canon/troubleshooting.md`, `canon/tidecloak-bootstrap.md`, `canon/anti-patterns.md`, `canon/security-gap-mapping.md`; playbooks (`deploy-tidecloak-docker`, `bootstrap-realm-from-template`, `initialize-admin-and-link-account`, `setup-iga-admin-panel`, `setup-forseti-e2ee`); bootstrap scripts (`templates/*/scripts/init-tidecloak.sh`, `templates/shared/bootstrap-tidecloak.sh`); reference-apps bootstrap-sequences + `iga-admin-governance`. **Bootstrap loop is REQUIRES_RUNTIME_VALIDATION** — verify end-to-end on a live iga-core instance. Historical `notes/` and `sources/` retain the legacy surface intentionally (audit record). |
| GAP-066 | Tide vendor surface on Skycloak-hosted TideCloak clusters unconfirmed | RESOLVED_BY_RUNTIME_TEST | Probed on a live hosted cluster 2026-08-06. `vendorResources/setUpTideRealm` **works** (creates the Tide IdP + `tide-vendor-key`, provisions the free-tier subscription, generates the gVRK; 10–15s because it really reaches Tide licensing). `tide-admin/toggle-iga` and `iga/change-requests` return 200. Adapter export carries `jwk`, `vendorId`, `homeOrkUrl`. Licensing needs no operator step. **Version-gated:** `0.13.13` ships a broken automation client, `0.14.11` fails licensing with `TIDE-IDPEXT-VENDOR-KEYGEN_FAILED` (isolated to license activation — `skipLicense=true` returns 200). `0.14.17` is the verified FLOOR. | Resolved. Recorded in `canon/hosting-options.md` (GAP-066 section) and `playbooks/provision-tidecloak-skycloak.md`. ⚠️ Resolution is **version-gated, not permanent**: do not hardcode `0.14.17` — discover the newest eligible tag with `templates/shared/skycloak-latest-version.sh` (AP-84). Only the floor is VERIFIED; newer versions are ASSUMED good until re-tested. |
| GAP-067 | No typed constructor surface for `Models.Policy` (and Models generally) via `@tidecloak/js` | STILL_UNRESOLVED | LEARNINGS-music-license-001 L-01/L-02: `Models` reaches apps through a dynamic import that must be cast (`as unknown as {...}`), so every app hand-writes its own `PolicyCtor` type. The Policy constructor validates `version`/`contractId`/`modelId`/`keyId`/`params` at runtime and throws BARE STRINGS (`'Version is not a string'`) on the first mismatch, sequentially - so a wrong shape costs one round trip per wrong field. The field it reads (`data["modelId"]`, singular) differs from the class property it populates (`modelIds`, plural), so the plural name is the one visible in editors and logs. A hand-written type describing the intended shape typechecks clean against a call that can never work. | Ship a `PolicyConfig` type (and Models types generally) from `@tidecloak/js`, and throw `TideError` with the accepted shape rather than a bare string. Until then, canon carries the field table. |
| GAP-068 | `tide_dpop_auth.html` is distributed by copy-paste, and older copies break the popup DPoP fallback | STILL_UNRESOLVED | LEARNINGS-music-license-001 L-03/L-04: the DPoP relay page ships as a static asset copied into each app's `public/`. Older copies post to `window.parent`, which is correct for an iframe and self-referential in a popup (`window.parent === window`), so `_popupDPoPFallback` never receives `pageLoaded` and tide-js reports "Popup DPoP verification failed to load". The popup is legitimately blank (the page is script-only with no markup), so the symptom looks like a load failure. 10 copies found on one dev machine at two different versions, with no version marker in the file and no staleness check. | Serve the page from the package or a versioned path; failing that, stamp a version into the asset and have the adapter warn when the served copy is older than the SDK expects. Fixed copies use `window.opener \|\| window.parent`. **Pack side MITIGATED 2026-08-07**: all four templates had shipped the STALE 7183-byte copy — canon (`invariants.md`) told operators "the only source is the pack template", so the pack was itself the distribution vector for AP-62. All four now carry the known-good 9120-byte asset (sha256 `9d7844b9…a757`), the Vite and vanilla templates gained the asset they were missing entirely while enabling DPoP in strict mode (a MISSING page 404s, worse than a stale one), and `scripts/check-dpop-asset.sh` hashes every copy under a tree against known-good/known-stale. Hashes recorded in `canon/invariants.md`. **Vendor side STILL OPEN, and wider than first recorded**: the asgard ASP.NET SDK itself ships a stale variant (7195 bytes) at `Tide.Asgard.AspNetCore.DPoP/Views/tide_dpop_auth.html` and `.Example/Resources/tide_dpop_auth.html`, so the bug is in a vendor-distributed SDK, not only in copy-pasted app assets. `sources/` copies left unmodified as audit record. |
| GAP-069 | No SDK helper for the Forseti contract transport; four apps hand-roll the same nesting | STILL_UNRESOLVED | LEARNINGS-music-license-001 L-09: submitting a contract requires a positional, untyped, three-level TideMemory structure - `[ "forseti", [ <empty>, [ source, "Contract" ] ] ]` - built by hand in every app (`ui-framework`, `ratifiy`, `keylessh`, `music-license`). `TideMemory.CreateFromArray` accepts any nesting, so omitting the outer level is not an error anywhere in the client; the ORKs answer `Unknown contract type ''` because they read position 0 of a structure that is one level too shallow. It fails at the LAST stage, so every attempt costs a full operator-approval cycle in the enclave. | Ship `buildContractTransport(source, entryType)` in the SDK. A shape four codebases duplicate by hand is a shape the library should own. Until then canon carries the nesting diagram. |
| GAP-071 | No unauthenticated endpoint publishes the realm VVK, so third parties cannot verify a signature independently | STILL_UNRESOLVED | LEARNINGS-music-license-001 L-18: the Ed25519 realm VVK is reachable only from `data/tidecloak.json` (the adapter export) or `/admin/realms/{realm}/vendorResources/get-tide-jwk` (master-admin token). The realm's OIDC JWKS serves Keycloak's RSA token-signing keys, so pointing a verifier there returns 200 with a valid key set containing the WRONG key - the failure looks like a broken certificate rather than a wrong URL. Any app issuing verifiable claims must therefore serve its own copy of the key, which comes from the issuer - the party a sceptical verifier is trying not to trust. | Publish the realm VVK on an unauthenticated, CORS-open endpoint the way OIDC publishes its JWKS. Until then apps serve `jwk.keys` from the adapter and state the weakness in the response. |
| GAP-073 | `[PolicyParam]` binding is only verified for `string` and `int`; `long`/`decimal`/`bool` untested | STILL_UNRESOLVED | LEARNINGS-tidewater-001 L-06: every documented `[PolicyParam]` example binds a `string` or an `int` (`Default = 2, Min = 1, Max = 10`). Whether the binder handles `long`, `decimal` or `bool` is unknown, and a binding surprise does not fail politely — it surfaces as a contract failure at request time, ON THE ORKS, after an enclave approval has been spent. This bites hardest on money: amounts in cents overflow `int` at $21,474,836.48, which is fine for a demo and not for a corporate ceiling. | Vendor: confirm which CLR types `[PolicyParam]` binds, or document the supported set. Until then canon prescribes declaring 64-bit values as `string` and parsing them in-contract with a hand-written digit loop — which also buys determinism, since rejecting signs/separators/whitespace/leading zeros means no culture setting can change what a number means on one ORK versus another. That is a workaround, not an answer. |
| GAP-072 | Custom-model identity: TWO consumers key off DIFFERENT fields, so IMPLICIT and EXPLICIT flows need different constructions | PARTIALLY_RESOLVED_BY_MULTI_EXEMPLAR | LEARNINGS-music-license-001 L-11/L-13 plus source reading 2026-08-07. A custom model's identity is expressed in three places with three conventions: `BasicCustomRequest.id()` in `asgard-tide` returns `` `BasicCustom<${name}>:BasicCustom<${version}>` `` (VERIFIED, `src/models/CustomTideRequest.ts`); tide-js's `ModelRegistry` matches a DIFFERENT wrapper `/^Custom<(.*)>$/` for the enclave's human-readable rendering path, and `CustomSignRequestBuilder._id` STRIPS it; sibling classes wrap as `DynamicCustom<...>` / `DynamicApprovedCustom<...>`. Reasoning from the rendering path to what the policy should declare is a recorded wrong turn (L-13). Unresolved detail: L-13 records the working git-signing app passing ALREADY-WRAPPED strings (`"BasicCustom<GitSign>"`) into the constructor, which per `id()` would double-wrap to `BasicCustom<BasicCustom<GitSign>>:…` and not equal the policy's `modelId` — so either that recollection is off or the ORKs key on the raw `name`/`version` fields rather than `id()`. Not resolvable from client source; the ORK-side .NET registry is not readable from here. | **Mechanism RESOLVED 2026-08-10** by reading `BaseTideRequest.encode()` (`"modelId": te.encode(this.id())`) plus live evidence. The wire `modelId` is whatever `request.id()` returns, and there are two equivalent constructions: `BasicCustomRequest("Name","1")` from `asgard-tide` (`id()` wraps) and `BaseTideRequest("BasicCustom<Name>","BasicCustom<1>")` (`id()` = `name+":"+version`). Both yield the identical id, which is why L-13's record of pre-wrapped constructor args was **not** self-contradictory. `BasicCustomRequest` is not exported from the `@tideorg/js` Models barrel, so `asgard-tide` is its only import path — making the `BaseTideRequest` form a legitimate workaround. **VERIFIED IN PRODUCTION**: reference app `attested-provenance-registry` has a threshold-signed policy with `modelId` `BasicCustom<OriginAttestation>:BasicCustom<1>` and 4 attestations signed under it. **Residual gap** (why this is not fully resolved): the three conventions still coexist with no client-side cross-check, and the ORK-side .NET registry remains unreadable from here. Vendor should expose one helper deriving request identity and policy `modelId` from a single constant. Canon prescribes asserting `request.id() === policy.modelIds[0]`, which is correct under either construction. **MECHANISM NOW EXPLAINED (2026-08-11, LEARNINGS-deploy-gate-001 L-03/L-03a/L-04)** — and it corrects a pack error. TWO consumers read the request and key off DIFFERENT fields: the ORKs read `request.id()`, while the enclave approval CARD reads the raw `name`/`version` fields (`ModelRegistry.getHumanReadableModelBuilder` does `BaseTideRequest.decode(data)` — returning a BaseTideRequest, so `r.id()` is `name+":"+version`, NOT the subclass's wrapped id — then matches `r.name` against the ANCHORED `/^Custom<(.*)>$/`). Canon previously called two constructions "equivalent" because they yield the same `id()`; they differ in the raw `name` field and **both fail the card**, so that claim was wrong and is corrected. NETWORK-VERIFIED: a bare `Custom<X>:Custom<1>` model id is **rejected by all 20 ORKs** at `executeSignRequest`, after the approval was spent (cost one approval to learn). SOURCE-VERIFIED: only `BasicCustomRequest` with name/version **pre-wrapped in `Custom<...>`** satisfies both consumers — raw name `Custom<X>` renders the card, and `id()` becomes the double-wrapped `BasicCustom<Custom<X>>:BasicCustom<Custom<1>>` for the ORKs. **STILL UNVERIFIED**: whether the ORKs accept that double-wrapped id — it matches the `BasicCustom<…>` shape they are known to accept, but has not been signed. Since `ValidateApprovers` runs ONLY under `ApprovalType.EXPLICIT`, and EXPLICIT always builds a card, **every multi-approver custom contract depends on this shape**. keylessh is not a counter-example: it pre-wraps with the wrong inner token (`BasicCustom<…>`) and is IMPLICIT, so no card is ever built (AP-77). | Vendor: state which field the ORKs key on and ship one helper deriving request identity and policy `modelId` from a single constant. Pack: one successful EXPLICIT deployment closes this. Until then canon carries both assertions (`/^Custom<.+>$/` on the name, `/^BasicCustom<.+>:BasicCustom<.+>$/` on `id()`) so a wrong guess fails client-side. |
| GAP-070 | WITHDRAWN - not a version skew. The ORKs are V3-only (`Could not find specified policy version: 2`); the `ReadOnlyCollection` in their model-lookup error is an ORK-side message-formatting artifact that prints the whole model collection instead of the failing entry. Real cause was an unregistered model id (LEARNINGS-music-license-001 L-13). Worth keeping as a documented near-miss: an error that quotes a .NET type name does NOT reliably mean a layout mismatch. | WITHDRAWN | LEARNINGS-music-license-001 L-12: V3 `toBytes()` writes the model id at slot 2 as a TideMemory COLLECTION (`01 00 00 00 13 00 00 00 "OriginAttestation:1"`); V2 writes a bare string. All 20 ORKs answer `Model id 'System.Collections.ObjectModel.ReadOnlyCollection\`1[System.String]' not found in registry` - the message prints the VALUE it read, so the network is reading that slot as a string and getting a collection. Every published @tideorg/js (0.14.12-0.14.20) has `latestVersion = "3"` and the constructor forces it when `new.target === Policy`, so there is no supported way to emit V2; a subclass overriding `toBytes()` works and is a workaround, not a fix. Blocks ALL custom Forseti policy deployment, and fails only after an operator approval in the enclave. | Confirm with the vendor whether the deployed ORK network is expected to accept V3 policies. Either upgrade the network or have the SDK emit the layout the network reads. |

---

## Status Summary

| Status | Count |
|--------|-------|
| RESOLVED_BY_KEYLESSH | 1 |
| RESOLVED_BY_VENDOR | 34 |
| RESOLVED_BY_NORMATIVE_DOC | 3 |
| RESOLVED_BY_MULTI_EXEMPLAR | 2 |
| PARTIALLY_RESOLVED_BY_KEYLESSH | 4 |
| PARTIALLY_RESOLVED_BY_MULTI_EXEMPLAR | 4 |
| PARTIALLY_RESOLVED_BY_VENDOR | 0 |
| RESOLVED_FOR_EVALS | 1 |
| RESOLVED_BY_WHITEPAPER | 1 |
| RESOLVED_BY_RUNTIME_TEST | 1 |
| STILL_UNRESOLVED | 14 |
| DOCS_ONLY_CONTRADICTION | 0 |
| RESOLVED_FOR_TEMPLATES | 1 |
| REQUIRES_RUNTIME_VALIDATION | 0 |
| **Total** | **66** |

Red-team field-test corrections (2026-07-22, second pass): first live run of `tide-red-team` against a real TideCloak application surfaced two canon defects and one skill defect.

- **I-12 is wrong for the shipped SDK. CORRECTED.** I-12 stated DPoP is "ENABLED and ENFORCED by default … `useDPoP` defaults to `{ mode: 'strict' }` … opt-OUT, not opt-in". Verified against an installed `@tidecloak/js` / `@tidecloak/react` **0.13.27**: `js/dist/cjs/lib/tidecloak.js` assigns `this.useDPoP` only inside `if (initOptions.useDPoP)`; `js/dist/cjs/src/IAMService.js` initialises DPoP only `if (this._config?.useDPoP)`; no `mode: 'strict'` default exists anywhere in the package. **In 0.13.27 DPoP is opt-IN, and omitting it yields plain bearer tokens with no error.** I-12 now carries a version warning and a verification command. **Re-confirmed unchanged on the 0.14.x line** (verified against installed 0.14.9; confirmed for the pinned 0.14.11, 2026-07-30): still opt-in, same `if (initOptions.useDPoP)` gate, no strict default. This defect is dangerous in the unsafe direction — a team reading it would believe a control was active when it was not, which is exactly what the reviewed application did.
- **Role sourcing was under-specified. Skill corrected.** Canon establishes the correct pattern (I-03/I-08, `feature-mapping.md` "Role-based API authorization ✓ threshold-verified roles") but nothing stated plainly that a *server-side role table* is a finding rather than a control. A DB-backed role check looks more rigorous than trusting a claim and is weaker here: it is a second source of truth with no cryptographic protection and no quorum gate on changes. `tide-red-team` now says so explicitly, with a detection command.
- **Skill allowed silent surface skips.** The eight-surface App Scrutiny Pass was prose with no required output, so a skipped surface was invisible — the report looked clean because the check never ran. Token binding (SG-03) was missed on the first live run for exactly this reason. The pass now requires a recorded verdict per surface (FINDING / PASS / N-A / NOT CHECKED) with `file:line` evidence, and a PASS without evidence is not a pass.

Also added to the skill: report language rules (translate internal terminology for director-level readers) and cost-framing rules (express impact in the reader's own units; never present derived figures as measured).

Red-team mechanism sync (2026-07-22): canon `breach-precedents.md` (~70 sourced incidents) and `tide-neutralization.md` (six mechanism classes, quoted from `sources/whitepaper/`) added; skill `tide-red-team` reworked for app scrutiny. Three canon corrections raised, none requiring a new GAP:

- **I-09 honest-minority bound is wrong.** I-09 states security holds if *"fewer than 30% of ORKs compromised… On mainnet: <7 of 20."* `tier1-article9` gives three zones: **0–6** normal, **7–13 DoS only** (*"cannot reconstruct the key"*), **14–20** reconstruction; the requirement is `n − t + 1` ≥ 7 honest. I-09's figure is the no-impact zone, not the security bound, and understates the confidentiality margin (13, not 6). **Action: correct I-09.**
- **Source defect, not a pack defect.** `tier2-protocol-prism` labels a row *"13-of-20 security tolerance"* while defining it as `n − t` (= 6). `tier1-article2` gives the correct split. Prefer Article 2; do not propagate the PRISM table's formula.
- **Source conflict, tier-1 vs tier-2.** Article 5's diagram says VVK ORKs *"Strip JWT to proof-approved claims"*; `tier2-protocol-vvk-jwt-signing` says *"The ORK does not modify or strip the JWT; it validates and either accepts or aborts."* Prefer tier-2 (more specific).

Confirmed as `DOCUMENTED_GAP_IMPLEMENTATION_COVERED` rather than gaps (whitepaper-absent, implementation-established): the `max(1, floor(N*0.7))` quorum arithmetic (`SetupIGA.md`, I-10); Roslyn + `AssemblyLoadContext` as Forseti sandbox layers (vendor confirmation, GAP-008, I-15); relying-party JWT verification and embedded JWKS (I-03/I-04, exemplar repos); CSP `frame-src` for the SWE iframe (I-06). All four are absent from `sources/whitepaper/` — attribute them to the implementation record, never to the whitepaper.

Genuine gaps found in the whitepaper and left unfilled (no new GAP raised; each is marked inline in `canon/tide-neutralization.md`): concrete proactive-resharing interval; mechanism of the enterprise admin-quorum recovery variant; behaviour when a user supplies fewer distinct recovery addresses than the threshold; consequence of a *captured* (vs lost) admin quorum in Ragnarök; the Phase-3 window in which TideCloak holds the assembled VVK; a stated residual for Forseti block-list completeness; user click-through on the Authenticator App's unfamiliar-TLD warning. The addendum and protocol doc also **conflict on DVK generation** (Nested Shamir DKG vs local secure-element keygen) — prefer the protocol doc.

Security-analysis sync (2026-07-06): GAP-065 added for the IGA/QEA API-surface + Tide/Tideless mode-split divergence between pack canon and current staging. Canon `security-gap-mapping.md` + `security-runtime-probes.md` and skill `tide-security-analyst` added.

Hosting-integration sync (2026-07-07): GAP-066 added for the unconfirmed Tide vendor surface on Skycloak-hosted TideCloak clusters. Canon `hosting-options.md` and playbook `provision-tidecloak-skycloak.md` added.

IGA API-migration sync (2026-07-07): GAP-065 reclassified STILL_UNRESOLVED → PARTIALLY_RESOLVED_BY_VENDOR → **RESOLVED_BY_VENDOR** once the iga-core spec (`qea-iga-api.md`) was obtained. New authoritative reference `canon/iga-change-requests-api.md`; the `/iga/change-requests/...` surface reconciled across canon, playbooks, bootstrap scripts, and reference-apps. Bootstrap approve→commit loop is REQUIRES_RUNTIME_VALIDATION. `PARTIALLY_RESOLVED_BY_VENDOR` status added to the controlled set (currently 0). Historical `notes/`/`sources/` intentionally retain the legacy surface.

Full sync pass (2026-04-13): GAP-001 upgraded from PARTIALLY_RESOLVED_BY_KEYLESSH to RESOLVED_BY_VENDOR (all auth endpoints now documented in `canon/tidecloak-endpoints.md`). GAP-029 upgraded from PARTIALLY_RESOLVED_BY_MULTI_EXEMPLAR to RESOLVED_BY_VENDOR (`get-required-action-link` schema now documented). RESOLVED_BY_VENDOR count corrected to 32. No new gaps.

Integration-focus sync (2026-04-13): 4 new gaps added (GAP-058 through GAP-061) for server-side delegation, endpoint documentation, the asgard-tide delegation SDK, and scoped delegation. Canon updated with `tidecloak-endpoints.md`. (The server-side delegation canon + playbook added in this pass were removed 2026-07-07 as unshipped; GAP-058/060/061 reopened as UNSHIPPED_REMOVED.) See `notes/test-findings/tidecloak-integration-focus.md`.

Batch-01 adjudication (2026-03-25): 14 gaps resolved by vendor answers.
Batch-02 adjudication (2026-03-25): 5 gaps resolved by vendor answers. GAP-029 strengthened. 5 assumptions resolved (A-21, A-22, A-25, A-26, A-28).
Post-adjudication sync (2026-03-25): GAP-007 reclassified (gas metering covered by GAP-008 resolution). GAP-033 corrected (jwk conditional on IGA). GAP-018 strengthened (ORK error codes now documented). GAP-038 reclassified (content-type asymmetry is documented behavior). Stale "Incorporate into canon" next actions updated to "Canon updated". Total resolved: 32 of 57.
Whitepaper review (2026-03-30): GAP-015 reclassified from STILL_UNRESOLVED to RESOLVED_BY_WHITEPAPER. Correct curve name is BEd255475; Architecture.md had a typo. Total resolved: 35 of 57.
Template-blocker resolution (2026-03-25): GAP-034 reclassified from DOCS_ONLY_CONTRADICTION to RESOLVED_FOR_TEMPLATES. Template guidance for threshold, image, proxy, HTTPS settled in `notes/template-blocker-resolution.md`.
Eval-blocker resolution (2026-03-25): GAP-018 reclassified from PARTIALLY_RESOLVED_BY_MULTI_EXEMPLAR to RESOLVED_FOR_EVALS. Client-visible error catalog sufficient for eval pass/fail. A-27 resolved for eval purposes (assume independent, soft assertions). See `notes/eval-blocker-resolution.md`.

Forseti policy-deployment sync (2026-08-07): GAP-067 through GAP-071 added from `notes/test-findings/LEARNINGS-music-license-001.md`, recorded while driving a real policy deployment to completion in `~/music-license`. Nine findings, every one from a real error message rather than a code review. Highest-value artifacts are the Policy constructor field table (L-01) and the contract-transport nesting diagram (L-09) - together they convert a multi-round trial-and-error loop into a copy-paste, and one of those rounds costs an enclave operator approval because it fails at the threshold-signature stage. Recurring theme across L-01, L-03, L-05 and L-07: every layer reported a symptom instead of the cause it already knew (a bare-string throw, "failed to load" on a page that loaded fine, one generic client message covering three distinct server failures). Making each layer say what it knows would do more for Tide app DX than any new feature. Also noted: the realm admin policy is named `tide-realm-admin`, not `admin-policy` (L-06), and code finding it via a `?? policies[0]` fallback only works while exactly one policy exists.

Reteaching the root cause (2026-08-17, latest): asked whether the pack had actually been *retaught* rather than patched, the honest answer was **partially** — the four instances were fixed and the strings gated, but the **reasoning error** that produced them was untouched, and it lives in the pack's own governing instruction. `CLAUDE.md` said *"Treat `sources/` as the main authority"* full stop. Whoever inverted T-26 followed that rule **correctly**: compared a shipped template against the `sources/example-app-keylessh` exemplar, found a difference, called it drift, reverted the working file to match, and recorded the conclusion as VERIFIED. The rule is right for **how Tide works** (API surface, protocol, security model) and wrong for **the version of a shipped artifact**, because a vendored repo is a snapshot that can be stale and can hold several copies of one file — keylessh holds two `tide_dpop_auth.html`, and its repo-root one is the broken one. `CLAUDE.md` now carries that qualification and the deciding rule: **"differs from the exemplar" is not evidence of corruption — it is equally evidence the exemplar is old; correctness is decided by BEHAVIOUR against the current runtime, not by provenance, size, age or exemplar-matching.** New **AP-83** records the concrete inversion, the wrong-vs-right table, and the detection lesson. Auditing the existing protection found the file-level gate was already sound and bidirectional (reference = the *good* `tidecloak-test-cases` copy, plus an explicit `window.opener` direction-guard) — what was missing was prose gating, the reasoning lesson, and a way to obtain the file at all. Two further hardenings: the drift failure message said *"copy the exemplar verbatim"* — the exact phrasing that caused the inversion, shown at the moment someone is about to repeat it — now replaced with decide-by-behaviour guidance naming the correct source and warning off keylessh; and a **known-good sha256 anchor** was added, because "all copies agree" could otherwise mean "all copies are equally wrong". Verified all four reversion directions now fail: a template reverted to stale (1 failure), the reference reverted (6), **every copy swapped at once** (2 — caught only by the hash anchor), and prose pointing at the stale path (1). 151/151.

DPoP relay asset — user report (2026-08-17): a real setup report identified two issues, and investigating them found the pack was **causing** the second. **Issue 1 (genuine gap)**: no MCP tool served `tide_dpop_auth.html`. The file is not in the `@tidecloak/*` packages and not in the container, so the user searched GitHub — and the obvious path in `tide-foundation/keylessh` is the **stale** copy. **Issue 2 was the consequence**: that copy posts to `window.parent`, which in a popup (`window.open()`) is the popup itself, so the enclave never receives `pageLoaded` and login failed with **`TIDE-SWE-UNHANDLED`**. They fixed it with `window.opener || window.parent` — arriving independently at exactly the pack's known-good file. **Issue 1 caused Issue 2, and fixing Issue 1 prevents it.** Worse, following the pack literally would have produced the same failure: `playbooks/add-auth-nextjs-fresh.md` named `sources/example-app-keylessh/public/` as the canonical source (the stale copy — keylessh carries **both**, and its `client/public/` holds the good one), instructed *"do not add popup/`window.opener` handling"*, and its verification asserted `! grep -q "window.opener" && echo "correct copy"` — which **fails the correct file and passes the broken one**. `canon/troubleshooting.md` T-26 carried the same inversion plus its origin: a 2026-08-06 note concluding *"both pack templates had silently drifted to a modified 9121-byte variant while the exemplar held the canonical 7183-byte file"* — exactly backwards, and the reason the templates had been reverted **to the broken file** (which is the state this session found them in). `skills/tide-integration/SKILL.md` had it too, and was found only by the new regression gate. All four corrected; T-26 now carries an explicit REVERSED note so nobody "fixes" it back. New MCP tool **`tide_dpop_asset`** closes Issue 1 by returning the file **byte-exact** (verified: 9120 bytes, sha256 `9d7844b9…a757`, byte-identical to the pack template over the wire) together with the rewrite/CSP wiring, the three-cause triage for `TIDE-SWE-UNHANDLED`, and a warning that the file **ends without a trailing newline** — copy-paste routinely adds one, changing the hash on a file the enclave integrity-checks (caught by round-tripping the tool's own output). New content gate 1g: nothing may source the file from the stale keylessh path or assert `window.opener` is absent; `canon/troubleshooting.md` is exempt because its reversal note quotes the wrong instruction in order to correct it. **The lesson**: `scripts/check-dpop-asset.sh` hashes FILES and reported all four templates green, while four separate documents told readers to go fetch the broken one — a file-integrity check cannot catch prose pointing elsewhere. 150/150.

Turnkey enclave branding (2026-08-13): the branding work shipped a generator and docs but the MCP tool returned *instructions*, so an agent would describe branding rather than do it. Now `templates/enclave-branding/brand-tidecloak.sh --realm <realm>` does the whole thing in one command — generate → validate → upload both → **save + re-sign** → verify — and `tide_branding` gained optional `realm`/`accent`/`appName`/`tidecloakUrl` inputs so it returns that command **pre-filled and ready to run**, leading with "RUN THIS, do not just describe it". The tool stays `readOnlyHint: true` (the pack's whole MCP surface is read-only for connector-directory safety); the agent executes the shipped script. The two Next.js init scripts now run it automatically during bootstrap, non-fatally, with `BRAND_SKIP=1` / `BRAND_ACCENT=<hex>` overrides — so a generated app arrives branded without the user asking. **VERIFIED END TO END on a live Tide realm** (`noted`, `iga.attestor=tide`): uploads returned real SHA-256 hashes, `set-branding` answered *"Tide branding updated and settings re-signed successfully"*, `get-branding` returned versioned URLs, the public `images/{LOGO,BACKGROUND_IMAGE}` endpoints served `200 image/png`, and the **served bytes were byte-identical to the generated files** with the URL's `?v=` hash matching the content hash — so the cache-busting is honest. Re-running is safe and deterministic (identical hashes on a second run). Two defects found by testing rather than review: (1) the script uploaded **both images before** discovering a realm could not be branded, leaving orphaned files on disk — now a fail-fast pre-flight; and (2) the first pre-flight checked the **Tide IdP**, which is the WRONG check — measured, an unlicensed realm returns **200** for `identity-provider/instances/tide` while having **no** `tide-vendor-key` component, so the check passed and the save still failed after two wasted uploads. The pre-flight now checks `tide-vendor-key`, precisely what `set-branding` filters on in `VendorResource.java`, and reports the licensing fix. Failure paths verified: unlicensed realm → exit 1 with zero uploads; nonexistent realm → exit 1; missing `KC_BOOTSTRAP_ADMIN_PASSWORD` → exit 1 before any work. Orphan files created by the pre-fix test were removed from the container.

Tidification compatibility / EdDSA gate (2026-08-13): **the pack was giving false information about migration.** `playbooks/migrate-from-existing-auth.md` stated *"No code changes needed. Same SDK, same OIDC protocol. The application does not need to know it is talking to TideCloak instead of Keycloak"*, plus *"Existing JWT verification code works without modification"* and *"These improvements are invisible to the application. No code changes required to benefit"*. All three are wrong, and the reason is EdDSA. MEASURED on `tideorg/tidecloak-dev:latest`: a Tide-enabled realm reports **`defaultSignatureAlgorithm: EdDSA`** and carries an Ed25519 (OKP) signing key, while non-Tide realms (`master`, `myrealm`) report **`RS256`** with no Ed25519 key at all; app clients carry no explicit signature attribute so they **inherit the realm default**, making every token `alg: EdDSA`. This is not revertible — Tide's signing *is* threshold Ed25519, and forcing RS256 would put a whole signing key back on the TideCloak server, i.e. remove the property being migrated for (I-02, I-09). **The verifier changes; the realm cannot.** VERIFIED blockers, by inspection rather than assertion: Node **`jsonwebtoken`** has **no EdDSA at all** (allowlist `HS*/RS*/ES*/PS*`, zero occurrences of `EdDSA`/`ed25519` in the shipped bundle) and is one of the most widely used JWT libraries; **`jwks-rsa`** pairs remote-JWKS fetching with RS256 (also I-04/AP-01); stock .NET **`Microsoft.IdentityModel.Tokens`** ships none — T-23, and `Tide.Asgard.Core` exists precisely to supply an EdDSA `SignatureProvider`; Node **`jose`** does support it and is the path the pack prescribes. Second, sneakier failure mode: a capable verifier **pinned** to RS256 — pinning is correct hygiene (SG-13), so the fix is to **repin to EdDSA, never unpin**. The pack deliberately **asserts nothing about specific gateways/authorizers/SaaS**, since support is version- and edition-dependent and a wrong claim is worse than none — instead it says to test the real deployment with a real EdDSA token, and names that out-of-repo layer as the blocker most migrations die on. New `canon/tidify-compatibility.md` carries the full gate including the non-EdDSA constraints (no headless/service-account identity GAP-063/064, browser-only signing, DPoP's four pieces, adapter JWKS, `/auth/redirect`, asgard+IGA incompatibility, `@tidecloak/*` ≠ keycloak-js per AP-69) and a three-way verdict vocabulary — **FULLY / PARTIALLY / NOT TIDIFIABLE** — with the rule that a partial migration is legitimate but presenting it as complete is the failure, because the excluded surface keeps its original exposure. New `templates/tidify-preflight/check-tidify.sh` makes the gate executable: verified exit 0 on `music-license` (a working Tide app) with its three `createRemoteJWKSet`/`certs` mentions correctly reported as *"documented, not used"* — comment hits are notes, because a checker that punishes a codebase for documenting the rule teaches people to ignore it — and exit 1 with 3 blockers plus a named excluded machine-to-machine surface on a synthetic legacy Keycloak app. Two script defects found and fixed while testing: an RS256-pin regex that required double quotes and so **missed the commonest JS form** `algorithms: ['RS256']`, and unclipped output that dumped a single 148 KB generated line. New AP-82, troubleshooting error-text rows for the post-migration 401 storm, an MCP standing instruction, and a content gate (147/147) that fails on any reintroduced "no code changes needed" / drop-in claim — which is how the two additional overclaims at lines 146 and 154 were found after the first fix.

Enclave branding assets (2026-08-13): the pack documented the branding *endpoint* and the `logoURL`/`imageURL` fields but **nothing about producing the images** — no format allowlist, no size cap, no dimensions, no padding — so an agent asked to skin the enclave had to guess, and the most natural guess (an SVG) is **rejected server-side**. Contract now VERIFIED by reading `tide-IdP-keycloak/.../TideIdpAdminRealmResource.java` and `tidecloak-key-provider/.../VendorResource.java`: `fileType` is `LOGO` or `BACKGROUND_IMAGE`; formats are an **allowlist keyed off the FILENAME extension** (`png,jpg,jpeg,gif,webp`) with **SVG rejected**; the cap is **5 MB** (413 beyond); **no dimension validation exists at all**; upload returns `{hash,name}` for a versioned cache-busting serve URL; one file per type, replaced on re-upload; and `POST vendorResources/set-branding` is **save AND re-sign atomically** (writes `ImageURL`/`LogoURL` then re-signs the settings blob the enclave verifies, 500 rather than leaving it unsigned) — so `sign-idp-settings` is not called separately for branding. Also found and documented: branding is **IGA-EXEMPT** (`IGA_VENDOR_PROVISIONING`), one of the few admin writes needing no authorize/commit drain, so it can be fixed **after** the one-way multiAdmin flip without an enclave ceremony. New `templates/enclave-branding/`: a **dependency-free generator** (`make-branding.py`, Python stdlib zlib+struct writing valid RGBA PNGs — no image model, no Pillow, no network) because **most coding agents cannot produce image files** and a text prompt yields nothing, a broken file, or an SVG; a **validator** (`check-branding.py`) that parses PNG/JPEG/GIF/WebP dimensions with no deps and separates **HARD** (server-enforced → exit 1) from **REC** (rendering recommendations → warnings), including transparent-padding detection via border alpha; and `IMAGE-PROMPT.md` for agents that *can* generate images. Recommended geometry is marked **ASSUMED** and reasoned, not asserted — nothing validates dimensions and the enclave's layout is not in readable sources: logo **512×512 PNG, alpha, ~12% transparent padding per side** (scaled under `object-contain`, so padding is the safe area), background **1920×1080 16:9 with a quiet low-contrast centre** (the login card sits on the middle — a busy centre is the failure that actually ships). Verified: output parsed back with a from-scratch PNG reader (valid signature, **all chunk CRCs correct**, 8-bit RGBA), outer ring genuinely alpha-0, corners carrying intermediate alpha; validator HARD-FAILs an SVG and a 6 MB file and warns on zero padding, a 128 px logo, a square background and a JPEG logo. Adaptive antialiasing — supersampling **only along the rounded-rect boundary**, since a smooth gradient gains nothing and AA is worth paying for on a hard edge only (O(perimeter), not O(area)) — cut generation from **37 s to ~5 s** at identical output size. New MCP tool **`tide_branding`** plus a standing instruction, registered in the protocol suite's `EXPECTED_TOOLS` allowlist (which correctly rejected the new tool until declared). 146/146.

Red-team doctrine + admin-credential hygiene (2026-08-11): three corrections, two of them to claims the pack was actively teaching. **(1) TideCloak is NOT an identity concentration, and the red-team skill said it was.** Surface 1's rule "delegated to the IdP is NOT a Pass" is right for Keycloak/Auth0/Okta/Clerk/Cognito/Firebase — all of which **own a verifier** — and wrong for a Tide-enabled realm, which owns none: PRISM stores **no password hash anywhere** (threshold OPRF; no ORK, server or attacker learns the password) and JWTs are threshold-signed across the ORK network, *"not by TideCloak server"*. So delegating login to TideCloak does **not** concentrate IdP verification into one spot — the broker is not the trust root, which is the architecture's whole point. Verdict on a verified Tide-enabled realm: surfaces 1 and 2 **SOUND**, not CONCENTRATED. Kept honest by distinguishing **holds** from **verifies**: a Tide realm still keeps a Keycloak **directory** (usernames, emails, attributes, role mappings) — real PII exposure worth its own finding, but its compromise authenticates nobody — and the residual concentrations that *are* real are availability, metadata, admin-path position on hosted, and Tideless-mode governance (a core-2 finding, not core 1). The carve-out is **conditional and must be verified, not assumed**: an unlicensed realm, a live local password flow, or federation to an external IdP each put SG-01 back in scope, so the skill now ships the `browserFlow=tidebrowser` / `iga.attestor` / Ed25519-VVK checks to cite. SG-01 corrected to say the gap is a **verifier, not a directory**. **(2) `resetPasswordAllowed` is irrelevant on a Tide realm** — password reset happens in the **Secure Web Enclave**, so the Keycloak realm toggle describes a code path users do not recover through. Both symmetrical errors are now named: it is not a gap ("no recovery path" — false, recovery exists in the SWE) and **not a mitigation either** ("reset disabled, no reset-email surface — Pass"), the second being the more dangerous because it retires the surface without looking at it. Surface 8 now assesses the real mechanism (threshold email recovery: N ORKs dispatch independently, user clicks **T unique links** within **1 hour**, CMK never touched — so single-mailbox takeover does not yield takeover, and T-11 is genuinely answered) and points at the finding that IS live: **how many DISTINCT addresses did the user supply**, since fewer than T is permitted by construction and in the worst case one mailbox authorizes recovery (construction VERIFIED, threshold-collapse consequence INFERRED). **(3) The master-admin password is out of the init scripts and into `.env`.** The worst instance was `username=admin&password=password` inline in `get_token()` in both Next.js templates; `templates/shared/bootstrap-tidecloak.sh` used `${KC_ADMIN_PASSWORD:-password}`, which is the same defect — **a default password is a hardcoded credential with extra steps**, shipping to whoever runs the script next. All three scripts now load `.env`, read `KC_BOOTSTRAP_ADMIN_PASSWORD` from the environment, and **fail loudly when unset** (verified: exit 1 with actionable guidance); token requests use `--data-urlencode` so `&`/`=`/`+` in a password survive; `docker run` passes the expanded variable. New `templates/shared/.env.template` is the canonical file, and the MCP now carries the rule in its **standing instructions** and in **`tide_hosting`** (the tool called before any deployment), including that hosted Skycloak issues no admin password at all. **`.env` was not gitignored in any template** before this — fixed in all five that ship an env example, because telling someone to put a secret in an untracked-but-uncommitted file is only safe if it is actually ignored. AP-41 gained a corollary covering credential *storage*, not just usage. Three regression gates added and each verified to fail on reintroduction: a literal assignment in any active script/config; a copyable `-e KC_BOOTSTRAP_ADMIN_PASSWORD=<literal>` in docs (with `canon/anti-patterns.md` exempted, since that file's job is quoting wrong forms — my first version of the gate failed on AP-41's own ❌ block); and any template shipping an env example must gitignore `.env`. 144/144.

Forseti SDK surface correction (2026-08-11): a real ORK compile failure showed the pack's contract examples teaching an API that does not exist, and the compile harness's stubs endorsing it. **All corrected against the two working, deployed contracts vendored in `sources/`** (`example-app-forseti-crypto-quickstart/.../forsetiContract.ts`, `example-app-tidecloak-test-cases/.../forsetiDecryptionContract.ts`), which had been sitting in the pack the whole time while canon carried simplified fiction. Corrected: `ctx.Data` is a **nested `ReadOnlyMemory<byte>`** walked with `GetValue(i)`/`TryGetValue(i, out …)` — there is **no `DataItem` type**, no indexer, no enumerator, and `ctx.Data == null` is `error CS0019` because it is a struct; **six** using directives are required, not one (`Cryptide.Tools`, `Ork.Shared.Models.Contracts`, `System`, `System.Collections.Generic`, `System.Text`); a decision **cannot be inspected** — no `IsAllowed` on `PolicyDecision` or on the `Decision` chain, and no `.And()` combinator because chaining IS the conjunction; `ctx.Policy.ExecutionType`/`ApprovalType` exist and both references use them to refuse a wrong policy shape; `ValidateApprovers` is **optional** (the quickstart reference omits it entirely). **New and load-bearing**: encrypt and decrypt have **different payload layouts** — tags start at index **2** for `PolicyEnabledEncryption:1` and index **3** for `PolicyEnabledDecryption:1`, and reading the wrong offset silently collects the wrong strings as tags, so the contract then allows or denies on garbage. **The harness's own stubs were a source of the problem**: they typed `ctx.Data` as `byte[]` and gave `PolicyDecision` a public `IsAllowed`, and a `byte[]` stub is *worse than none* because contracts written against it compile locally and fail on the ORK — the exact false PASS the harness exists to prevent. Fixed, and made self-checking: `check.sh --self-test` compiles both deployed references AND asserts six single-discriminator must-fail fixtures still fail. That second half matters — positive fixtures alone could not detect the over-permissive stub, because **`byte[]` is implicitly convertible to `ReadOnlyMemory<byte>`** so both references compiled either way; and a single must-fail file with several errors reports nothing, since it keeps failing whichever protection is lost. Verified: reverting `ctx.Data` to `byte[]`, re-adding `IsAllowed`, or adding `Data` to `ExecutorContext` each now fail with the specific fixture named. New `check-docs.sh` compiles **every** full contract example in `canon/`, `playbooks/` and `reference-apps/` — it found three broken ones beyond those reported (a second `ctx.Data == null` in the E2EE playbook, a snippet missing its usings, and a git-pr example using a `.And(…)` combinator plus a 4-argument `RequireAnyWithRole` that no reference supports). All seven now compile. New AP-81. Also captured: **per-resource capabilities are IGA-governed**, so a contract that binds to one costs a human enclave approval **per share** — the workable split is structural checks in the contract plus an app ACL for per-resource access, behind one parameter so full binding can be re-enabled later, with the limit stated in the contract source rather than only in an architecture note.

`tide-roles-mapper` removal (2026-08-11): **the provider does not exist and the pack was still asking for it in one active place.** MEASURED against `tideorg/tidecloak-dev:latest` (the production image): `GET /admin/serverinfo` lists **24** openid-connect `protocolMapperTypes` and **none contains "tide"**. Importing a realm whose client declares `"protocolMapper": "tide-roles-mapper"` returns **`201 Created`** and the mapper is **silently dropped** — verified by re-reading `.../protocol-mappers/models` afterwards, where only the stock `Tide vuid` mapper survived. So a realm.json referencing it imports "successfully" and roles go missing from every token with nothing anywhere saying why. **The pack's three shipped realm templates were already clean**; the live defect was `evals/cases.yaml` EVAL-053, which *required* `tide-roles-mapper` in `pass_criteria` and listed its absence as a `failure_condition` — an eval that would have failed a correct template and rewarded a broken one. Corrected, and the case now asserts the opposite. **Two earlier pack claims are refuted**: LEARNINGS-batch-004 L-06 / RB-024 said the provider exists on prod and is dropped only on staging — disproved by that finding's *own* regression command, which predicted `Production: ["tide-roles-mapper"]` and returns `[]`; RB-024 is WITHDRAWN. LEARNINGS-batch-006 prescribed *using* `tide-roles-mapper` to fix missing role claims — the harmful half is struck through, the attribute-mapper half stands. Both notes annotated in place per the pack's refuted-finding convention. Documented the real mechanism, all **stock** provider types: roles arrive via the **default `roles` client scope** (`oidc-usermodel-realm-role-mapper` + `oidc-usermodel-client-role-mapper`, so no declaration is needed at all), and Tide's own claims via the **`tide-claims`** scope that `setUpTideRealm` provisions (`tide-uho` hardcoded-claim plus `tideUserKey`/`vuid` attribute mappers) — both confirmed on live Tide realms. New AP-80. **Durable gate**: two `mcp-server` content tests now fail if any active realm template declares a `"protocolMapper": "tide-*"`, or if a realm template stops mapping `tideUserKey`/`vuid` — so "remove the bad mapper" can never be satisfied by deleting the Tide claims wholesale. Both verified to fail in the right direction and then pass (142/142). `sources/` copies left unmodified as bounded audit record. Noted but not fixed: `evals/cases.yaml` has never been machine-parseable (pre-existing unquoted `{` at line 285, present in HEAD) and nothing in the pack parses it.

Deploy-gate sync (2026-08-11): `notes/test-findings/LEARNINGS-deploy-gate-001.md` applied — 8 findings from a release-signing build (Keycloak 26.7.0, mainnet ORKs, Next 16.3.0) that reached a compiled+parity-checked contract but **not** a successful deployment. **The headline is a correction to canon that the pack itself authored.** GAP-072's "two equivalent constructions" note was wrong: TWO consumers read a signing request and key off DIFFERENT fields — the ORKs read `request.id()`, the enclave approval CARD reads the raw `name`/`version` (`ModelRegistry` decodes to a *BaseTideRequest*, so `r.id()` is `name+":"+version`, then matches `r.name` against the ANCHORED `/^Custom<(.*)>$/`). The two constructions yield the same `id()` but different raw `name`s, and **both fail the card** — so they are not interchangeable, and "equivalent" was misleading precisely where it mattered. NETWORK-VERIFIED: a bare `Custom<X>:Custom<1>` model id is **rejected by all 20 ORKs** after the approval was spent. SOURCE-VERIFIED fix: `BasicCustomRequest` with name/version pre-wrapped in `Custom<...>`, giving the double-wrapped `BasicCustom<Custom<X>>:BasicCustom<Custom<1>>`. **Deliberately NOT recorded as closed** — the double wrap has not been signed, so it is labelled PREDICTED, and canon carries both client-side assertions so a wrong guess fails locally. This matters broadly because `ValidateApprovers` runs only under EXPLICIT, and EXPLICIT always builds a card: **every multi-approver custom contract depends on this shape.** Also: the ORK `ReadOnlyCollection` message-formatting artifact confirmed a **second** time as NOT wire-format evidence (the refuted-theory note in canon earned its keep and saved the diagnosis). **L-07 reverses a decision I made last session**: the sandbox scanner's comment false-positive is now fixed properly rather than defended — `templates/forseti-compile-harness/scan-sandbox.py` strips comments with a C#-aware scanner that tracks string state, so a contract documenting its own restrictions passes with WARNs while `var u = "http://x"; var t = DateTime.UtcNow;` on one line still FAILS. My earlier "stripping is inherently unsafe" argument identified a real hazard (a naive stripper hides calls after a `//` inside a string literal) and drew the wrong conclusion; the same fix is applied to `forseti-parity-tests` and both READMEs say so. **L-05 disproves a claim I added yesterday**: `setUpTideRealm` did NOT set `iga.attestor` on this build (absent after a successful call with a free-tier subscription created), so "sets it on current builds" is replaced everywhere by **assert → stamp if absent → re-assert**. New AP-75 (role-check helpers taking a bare role name — `tide-realm-admin` is on `realm-management`, so an app-client-scoped check can never see it, and a gate that cannot pass for its intended user is a design error), AP-76 (bare `Custom<X>:Custom<1>`), AP-77 (copying keylessh's constructor args — the pre-wrap is right, the inner token is wrong), AP-78 (`(Models as any).Tools!` — `Tools` is a sibling export, and the only use of `TideMemory` in the deploy path is attaching the VVK signature, so the throw lands *after* the network has signed), AP-79 (the destructive bootstrap Step 0). `templates/shared/bootstrap-tidecloak.sh` now **refuses to wipe** when the container hosts realms besides `master` unless `FORCE_WIPE=1` — verified against a live container hosting six. L-01's `secureFetch` relative-URL failure was already AP-74; added the second throw site (`tidecloak-dpop.js:390-393`) and confirmed the pack's server-side `htu` guidance already correctly says origin+pathname with **no query** (4 places). L-08's field-order/regex-scoping lessons folded into the parity template's config comments.

Tidewater sync (2026-08-10): `notes/test-findings/LEARNINGS-tidewater-001.md` applied — 9 findings from an escalating-quorum payment-authority build (Keycloak 26.7.0, mainnet ORKs, Next 16.3.0) that reached a **threshold-signed policy**. **L-01 is a correctness bug in the pack's own instructions and was measured directly, not transcribed.** `toggle-iga` reads the FORM parameter `isIGAEnabled`; a JSON body is accepted, parsed by nothing, and the missing parameter **fails open to `true`** — so `{"enabled":false}` returns `200 {"enabled":true}` and the realm attribute becomes `true`. Measured on a throwaway realm: it also ran a **Phase-6 ADOPT scan over 207 entities** (34 `ROLE`, 66 `CLIENT_SCOPE_CLIENT`, 37 `PROTOCOL_MAPPER`, …) and emitted a lockout warning ("Fewer than 2 distinct admin holders … Recovery path if locked out: the master-realm admin can always disable IGA on this realm via the master realm — there is no other recovery"). Two further behaviours found beyond the finding: disabling from an enabled realm is **refused while change requests are pending** (`{"error":…,"conflictingChangeRequestId":…}`, state unchanged — drain first), and `toggle-iga` alone does **not** set `iga.attestor` (it stayed unset on an unlicensed realm, so `setUpTideRealm` is what sets Tide mode). This mattered because **the wrong half of the pack was the half agents follow**: 8 files sent JSON, including `bootstrap-realm-from-template.md`'s teardown step, which said to *disable* IGA using the form that enables it, and the `hosting-options.md` / `provision-tidecloak-skycloak.md` "is this real TideCloak?" probes, which silently enabled IGA on `master`. All corrected to form-encoded with a response assertion; the probes replaced with a **read-only** `GET .../iga/change-requests` (VERIFIED 200 on TideCloak and `master`'s state untouched). **L-05 RESOLVES** the `REQUIRES_RUNTIME_VALIDATION` note in `canon/custom-contracts.md`: `PolicySignRequest` and `BaseTideRequest` are two **halves of one flow**, not alternatives — `PolicySignRequest.New(policy).addForsetiContractToUpload(source)` owns the three-level transport, then the enclave approval is decoded with `BaseTideRequest.decode()` and the admin policy attached to the **approved** request before execute. They never disagreed; each prior finding described one half. **L-03/L-04 promoted to AP-73/AP-74**: in a DPoP realm bare `fetch` to your own API is a silent 401, while `secureFetch` with the same relative path throws `Failed to construct 'URL': Invalid URL` from inside the SDK before any request (`tidecloak.js:626-627`, bare `new URL()` with no base — reproduced in Node). Together they are a trap with no safe-looking exit and the only correct combination is `secureFetch` with an **absolute** URL; the relative-URL fact was already in the pack as a sub-pitfall of AP-45, findable only by someone who already knew. New `templates/forseti-parity-tests/` (L-07) — dependency-free `node:test` parity suite asserting the app's declared values against the **contract source text**, verified against a real threshold-signed contract: it correctly flagged 3 blocked-namespace hits *all in a doc comment* (labelled as such — the scan reads comments deliberately, because a comment-stripper can hide a real call inside a string literal), and its deployed-`contractId` freshness check passed against the live policy and then failed loudly with both hashes once the comments were reworded, proving the point that a contract's identity is its exact bytes. GAP-073 added (`[PolicyParam]` binding verified only for `string`/`int`; declare 64-bit money as `string` and hand-parse). Also: Next 15.3.3 marked superseded by CVE-2025-66478 (templates already pin `^16.0.0`), `iga.attestor` guidance changed from "stamp it" to "assert it", `tideInvitable` + drain added to the scenario bootstrap sequences (L-09), and a pre-existing orphan code fence in `framework-matrix.md` removed.

Agent-quorum sync (2026-08-10): `notes/test-findings/LEARNINGS-agent-quorum-001.md` applied — 14 findings from driving a role-distinct approver quorum to a threshold-signed policy on Keycloak **26.7.0** / mainnet ORKs. **The DPoP relay + CSP wiring was the priority and is now stated once, completely, in the file agents actually follow**: `canon/framework-matrix.md` gained a **Browser Prerequisites** section — the three files that each fail *silently* (`silent-check-sso.html`, `tide_dpop_auth.html`, `app/auth/redirect/page.tsx`), the complete copyable `next.config.ts` (wildcard `/tide_dpop/:path*` rewrite, per-path CSP **plus `Allow-CSP-From: *`** for CSP Embedded Enforcement, static rewrite not a route handler, specific rule ordered last), curl commands to verify on the wire, and how to read the CSP error. Previously `framework-matrix.md` mentioned only `frame-src '*'` and never mentioned the relay page at all, which is why this was not seamless. **Measured, not transcribed**: served headers read off a live Next 15.3.3 dev server, plus Next 16.3.0 templates. **L-08 REFUTED** — the per-path CSP rule *does* win when ordered last (one CSP header per response, specific rule applied on the relay path); its proposed "collapse to one policy for all paths" would delete `Allow-CSP-From`, reintroducing the very failure L-07 diagnosed. Kept in the findings file with the refutation so it is not re-derived; the surviving lesson is the method — verify by reading the served header, never by reasoning about rule order. **L-14 measured**: master admin tokens live exactly **60 seconds** (`expires_in: 60`, `exp - iat == 60`), so `deploy-forseti-policy` Step 1's "does not survive a restart" badly understated it; on-demand server-side minting with 401-triggered re-mint is now prescribed for any flow with a human in it. **L-01/L-02 correct a harmful canon claim**: `CREATE_ROLE` and `SET_USER_ATTRIBUTE` are governed writes on this build, contradicting the vendor-confirmed ACTIVE/DRAFT table (GAP-041) whose automation sentence told agents to skip the step that makes roles real — replaced with the rule that always holds, *a 2xx means ACCEPTED, not APPLIED*. New: `canon/agent-authority.md` (L-11 — agents cannot hold Tide identities; the one-principal-per-role pattern, and the honest limit that session compromise equals approver compromise, with a `WorkloadAttestation` seam), `templates/shared/drain-change-requests.py` (L-12 — arguments not stdin, rounds, 412/409 tolerance; a heredoc drain silently reads nothing and yields a green log over an empty realm), AP-70/71/72. Also fixed: `tideInvitable` prerequisite for invite links (L-03), change-requests returns an **array** (L-04), trailing slash in `auth-server-url` (L-05), the two webpack fixes' failure signatures (L-06), `role-policies` is empty until `tide-realm-admin` is granted and that grant is a one-way door into multiAdmin (L-09), the two providers' differing prop shapes (L-10), and `/auth/redirect` in the quickstart's required files (L-13). Confirmations recorded: `contractHash` (SHA-256) ≠ `contractId` (SHA-512 uppercase); the adapter carries a real Ed25519 OKP VVK; the compile harness caught a blocked-namespace hit in a doc comment (reword the comment, do not weaken the scan).

Reference-app sync (2026-08-10): `reference-apps/attested-provenance-registry/` added from `~/music-license` (Origin) — the pack's first scenario **verified end to end against a live ORK network**. Live evidence: a threshold-signed policy (`[dataToVerify(451), signature(64)]`, stored 2026-08-07) and 4 signed attestations (3 CREATOR + 1 DERIVATIVE, all 64-byte Ed25519). This confirms, as fact rather than argument: the ORKs accept the V3 policy layout (GAP-070's withdrawal), a `BasicCustom<...>` model id is accepted (GAP-072 mechanism), uppercase SHA-512 `contractId` is correct, `Cryptide.Tools.Utils.GetEpochSeconds()` passes IL vetting in production, and `ctx.Data` arrives as exactly the draft bytes (the top open question in the app's own RUNTIME-VALIDATION doc). GAP-072 reclassified STILL_UNRESOLVED → PARTIALLY_RESOLVED_BY_MULTI_EXEMPLAR. **Deliberately still open**: successful signatures prove the contract's checks ADMIT valid requests, not that they REJECT invalid ones — skew rejection, vuid-mismatch refusal and tamper detection remain unproven in-repo and are Phase 7 of the scenario's bootstrap sequence. The scenario's reusable core is a custom contract that binds authorship (signer vuid at a FIXED OFFSET, byte-compared to the doken, because the sandbox has no JSON parser) and bounds time (symmetric skew vs each ORK's own clock — native request expiry alone is one-sided and does NOT prevent backdating), plus six-check third-party verification that publishes evidence rather than a verdict.

Relearn application sync (2026-08-07, latest): LEARNINGS-music-license-001 (L-01…L-20) applied to the pack. **The DPoP relay asset was the most serious finding, and it was a pack defect, not just an app one**: all four templates shipped the stale 7183-byte `tide_dpop_auth.html` while `canon/invariants.md` instructed operators that "the only source is the pack template" — so the pack distributed AP-62. All four templates now carry the known-good 9120-byte asset; the Vite and vanilla templates were missing it entirely while enabling DPoP in strict mode (a missing page 404s, which is worse than a stale one); `scripts/check-dpop-asset.sh` added; both hashes recorded in canon. Newly found while fixing it: the vendor's own asgard ASP.NET SDK ships a stale variant (7195 bytes) in `Tide.Asgard.AspNetCore.DPoP/Views/`, widening GAP-068 from copy-paste drift to a vendor-distributed defect. GAP-072 added for the three competing custom-model wrapper conventions (`BasicCustom<...>` request identity vs `Custom<...>` enclave rendering vs `DynamicCustom<...>`), including an unresolved inconsistency between L-13's record of the working git-signing app and `BasicCustomRequest.id()` — canon prescribes the `request.id() === policy.modelIds[0]` assertion, which is correct under either reading. GAP-067 and GAP-069 remain STILL_UNRESOLVED on the vendor side but are now fully mitigated in canon (Policy field table + throw-string map; transport nesting diagram + the verified `heimdall-tide` helper that already builds it). L-20 added to the findings file from a live report: `ReviewScreen.tsx` read `tide.tokenParsed`, a keycloak-js property absent from `TideCloakContextValue`, and `undefined ?? {}` rendered a confirmation screen with no identity and a live action button — no error anywhere. Recorded as AP-69 with a fail-closed rule, and as the extreme case of the pack's new "every layer reports the symptom" diagnostic principle. New: `canon/verifiable-claims.md`, `playbooks/deploy-forseti-policy.md`, `templates/forseti-compile-harness/` (self-verifying: `examples/FailingContract.cs` must fail), and an error-text lookup table at the top of `canon/troubleshooting.md`.

Attestation-verification sync (2026-08-07, later): LEARNINGS-music-license-001 extended to L-19 after driving a policy deployment to a SIGNED attestation and building third-party verification. GAP-070 WITHDRAWN (the ORKs are V3-only; the `ReadOnlyCollection` in their model-lookup error is an ORK-side message-formatting artifact, not a layout mismatch - a near-miss worth keeping, because "the error quotes a .NET type name" is NOT reliable evidence of a wire-format problem). GAP-071 added for the missing public VVK endpoint. Highest-value additions: the Forseti local-compile harness (L-17 - contracts are compiled by the ORK at request time, so a typo costs an operator approval; a stub project catches it in a second and should be a pack template), and the six-check verification model (L-15) with its two blind spots - the file is never checked against the hash unless someone passes the file, and no software can bind a key to a human.

Skycloak version-discovery sync (2026-08-18): GAP-066 corrected STILL_UNRESOLVED → **RESOLVED_BY_RUNTIME_TEST** — it was probed live on 2026-08-06 and the register was never updated, so it read as unresolved while `canon/hosting-options.md` said RESOLVED. New status `RESOLVED_BY_RUNTIME_TEST` added to the controlled set. Separately, the hardcoded cluster version `0.14.17` was replaced by discovery (`templates/shared/skycloak-latest-version.sh`, newest ≥ floor from Docker Hub tags) across the playbook, `canon/hosting-options.md`, and `skills/tide-setup`; recorded as **AP-84** and gated in `mcp-server/test/content.test.mjs` (gate 1h). Newest at the time of writing: `0.14.20`.

Skycloak version-source correction (2026-08-19): the 2026-08-18 fix replaced the hardcoded `0.14.17` with Docker Hub tag discovery. That was still wrong — Skycloak exact-matches against a server-side allowlist and ignores Docker Hub, so the newest published tag 400s and the walk-down loop silently lands on an old version. Corrected to query `GET /clusters/supported-versions?type=tidecloak` (the endpoint the pack previously claimed did not exist). Docker Hub retained only as a `--check` lag diagnostic. AP-84 rewritten; gates 1i/1j added.
