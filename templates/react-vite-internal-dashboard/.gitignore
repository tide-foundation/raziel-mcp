node_modules/
dist/
build/
.next/

# Secrets — operator credentials NEVER belong in the repo (AP-41).
.env
.env.local
.env.*.local

# Local TideCloak state (adapter export + H2 volume)
data/tidecloak.json
data/keycloakdb*
