{
  "name": "tide-agent-pack-mcp",
  "version": "1.9.1",
  "description": "MCP server exposing the Tide agent pack (canon, playbooks, skills, prompts, adapters)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && node test/run.mjs",
    "test:remote": "node test/remote-check.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@types/express": "^5.0.6",
    "express": "^5.2.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
